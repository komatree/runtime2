## Deprecated

This top-level `skills/` directory is a legacy stub kept only to prevent human confusion during repo cleanup.

Do not use skill files from this path.

The only authoritative local skill pack for `runtime2` is:

- [`.codex/skills/`](/home/terratunes/code/trading/runtime2/.codex/skills)

If repo-local skills are not auto-exposed in a Codex session, open the needed file from `.codex/skills/<skill>/SKILL.md` directly.
